# Supabase — service role so the ingest service can write positions/alerts.
# Leave unset to run in decode-only mode (frames logged, nothing persisted).
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
